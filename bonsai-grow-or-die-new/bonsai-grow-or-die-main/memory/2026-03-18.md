# Daily Memory — Wednesday, March 18, 2026

## BonsaiGame.tsx Full Code Review

Aaron sent zip `bonsai-grow-or-die_15---257f81dc.zip` for a full code review + production refactor of the Bonsai Grow or Die game. File labeled v15 but Aaron confirmed it's actually **v38** (download counter reset when he switched laptops). File pushed at 12:03am, no changes since.

**File stats:**
- `/bonsai-game/components/BonsaiGame.tsx` — 4,559 lines, ~500KB
- Contains a 264KB base64 PNG (BONSAI_LOGO) inline in the .tsx
- Entire game is one monolith component

### Critical Bugs Found

**1. Broken P&L state (lines 2602-2603) — CRITICAL:**
```js
const [pnlViewYear, setPnlViewYear] = [currentYear, null]; // NOT useState
const [showPerRoom, setShowPerRoom] = [false, null];        // NOT useState
```
These look like React hooks but are plain array destructuring. `setPnlViewYear` is `null`. The P&L year picker and per-room toggle are permanently broken — calling the setter would crash.

**2. saveState called every game tick — PERFORMANCE:**
At 64× speed: tick fires every 150ms → `JSON.parse(JSON.stringify(state))` deep clone + `localStorage.setItem(JSON.stringify(state))`. This is expensive and unnecessary on every tick.

**3. HMAC signing salt in client bundle — SECURITY:**
```js
const SIGN_SALT = "bonsai_grow_or_die_2026_" + "zen_happy_plants" + "_420_leaderboard";
```
Visible in client code. Anyone can forge leaderboard scores.

**4. `// @ts-nocheck` at top of file** — TypeScript completely disabled.

**5. `fetchLeaderboard` missing from useEffect dep array** — line ~614.

**6. `speedBonusMsRef` defined (line 517) but never used** — dead code.

### Architecture Issues
- 21 `useState` hooks in one component
- No `useReducer` — uses `JSON.parse(JSON.stringify(prev))` on every state update
- No `useMemo` anywhere for expensive calculations
- All 4,500 lines of UI inline in one render (no sub-components)
- All styling is inline `style={{}}` objects (new objects every render)
- `// @ts-nocheck` disables all type safety

### Key Architecture (for context)
- Game loop: `setInterval(processTick, 150-2000ms)` depending on game speed
- State: one large flat object (`state`) with rooms[], upgrades{}, achievements{}, financials, etc.
- Persistence: `localStorage` via `saveState`/`loadState`
- Leaderboard: `/api/leaderboard` Next.js API route with HMAC score signing
- Upgrades: per-room system with 6 tracks (lighting, irrigation, environmental, genetics, preroll, operations), 3 tiers each
- Rooms: 9 total, 2 pre-unlocked (Room 1 veg, Room 2 flower), rest cost $250K–$1.1M to unlock
- Game spans 2015–2026, simulates real AMR (wholesale cannabis price) data

### Questions Pending Aaron's Answer
1. Fix broken P&L year selector (requires proper useState placement)?
2. Move HMAC salt server-side to env var?
3. Keep monolith but clean it up (Option A) vs. split into files/hooks (Option B)?
4. Move base64 logo to `/public/bonsai-logo.png` with Next.js `<Image>`?
5. Migrate to Tailwind classes vs. keep inline styles?

**Status: Waiting on Aaron's answers before starting refactor.**

## Other Project Notes
- Aaron is on MST (Denver, CO)
- His download counter restarted when he switched laptops — not a version discrepancy
